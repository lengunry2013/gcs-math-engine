# log level config
logging:
  level:
    org:
      springframework:
        web:
          filter:
            CommonsRequestLoggingFilter: DEBUG

# sever port config
server:
  port: 8099
  #ssl:
    #key-store: classpath:keystore.p12  # Certificate location
    #key-store-password: 87654321   # Your password
    #key-store-type: PKCS12
    #key-alias: bigdaddy-alias       # Your alias